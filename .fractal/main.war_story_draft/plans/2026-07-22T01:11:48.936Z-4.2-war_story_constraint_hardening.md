# 4.2 War Story Constraint Hardening

## Objective

Continue the existing `WAR_STORY_DRAFT.md` deliverable rather than starting a
new post. Give the completed draft one substantive editorial pass: make the
eight-job recovery more concrete, keep the three repair actions independently
legible, and sharpen the lesson without adding any claim not supported by the
opening entry in `CHRONOLOGY.md`.

The recovered constraints remain authoritative: one English post, first person,
at most 280 characters, lowercase opening, no hashtags or ALL-CAPS phrase, no
URL or exclamation mark, no emoji except the mandated closing, and an exact
final line of `Building in public. 🤖`.

## Execution

1. Rewrite the post in place at the worktree root; do not add a title,
   explanation, checklist, image, or publication metadata to the file.
2. Preserve the measurable breakage (all 8 cron jobs) and express the recorded
   recovery as three actions: rebuild schedules, update identifiers across the
   three named project files, and verify all 8 jobs active.
3. Prefer concrete wording from `CHRONOLOGY.md` over abstractions. Keep the
   lesson practical and specific to recoverability, while avoiding any invented
   timing, cause, impact, or ownership claim.
4. Run deterministic checks for Unicode character count, exact closing,
   paragraph shape, forbidden hashtags/URLs/exclamation marks, uppercase runs,
   and unexpected emoji. Then manually compare every factual phrase with the
   chronology and re-read the result against `VOICE_PROFILE.md`.
5. Run the node test and lint scripts. Leave MoA, image generation, approval,
   `post_with_log.sh`, and all X writes untouched because this remains a draft
   task only.

## Delegation and feasibility

No child node will be spawned. The remaining work is a single-file editorial
unit whose source check and constraint validation are tightly coupled; none of
its parts has enough independent depth to justify a separate loop. The
worktree is clean, the prior commit is a normal completed iteration rather than
an interrupted backstop, and both time and cost commands report no limiting
budget, so there is no feasibility blocker.

## Post-Mortem

### Accomplishments

- Rewrote the existing draft in place from 234 to 222 Unicode characters while
  preserving the required Problem → three repairs → lesson arc.
- Made the recovery more concrete: every schedule was rebuilt, 8 job
  identifiers were updated across 3 files, and all 8 jobs were verified active.
- Re-derived the printed numbers from `CHRONOLOGY.md` and passed the length,
  exact-closing, paragraph, forbidden-token, uppercase-run, non-ASCII, test,
  and lint checks.

### Deviations

- Kept the mandated period and robot emoji in the exact closing even though the
  general voice profile prefers an open ending without punctuation or emoji;
  the recovered task-specific constraint is more specific.
- Did not add a persistent validator because this is a one-file prose artifact;
  the deterministic checks were run directly against the delivered bytes.
- Did not edit the project wiki because the pass produced no reusable
  architecture, convention, or cross-node pattern.

### Next-iteration notes

The standalone draft needs no further editorial work. Any image generation,
MoA scoring, approval, publication, or X write remains a separate explicitly
authorized workflow.
