Сначала загружу контекст и прочитаю драфт, затем сверю его с правилами.Сверю факты с хронологией и посчитаю длину — без этого вердикт будет на глаз.Длина 2502 — недобор. Ещё сверю, где факты сидят явно, а где только намеком.**Вердикт: PASS-WITH-FIXES**

Жанр правильный: это гайд, не колобок. Факты все на месте, секретов нет, тон без пафоса. До EN не готов: нет голоса строителя, нет закрытия, 3 шага нельзя повторить, 2501 знак при цели ~3000.

### Чеклист

**MUST**
1. Голос строителя, не README. Одна фраза в «Зачем», без сцены: *I'm not a programmer, I run a construction site. I was the human relay between agents. Here's how I took myself out.* Не добавлять «день первый был адом».
2. Закрытие. После уроков, не хештег: *Шина жива. Роутер надо проверять каждый день. Война не кончилась.* Убрать «13.08 — работает» — звучит как победа.
3. Kill-switch с командой. Сейчас «скрипт». Писать: `buzz-profile.sh stop-all` (gateway не трогает). Без имени шага нет.
4. Куда ставить `--reasoning none`. Сейчас флаг висит в воздухе. Писать: на вызове профиля / LLM CLI, не в `.env` Buzz.
5. Два poll не путать. `BUZZ_POLL_INTERVAL=5` — клиент. Роутер `buzz-message-router` — 4 с. Иначе инженер «исправит опечатку» и сломает одно из двух.
6. 5 агентов в «Что вышло», не только в уроке 2. Сейчас штаб не виден.

**SHOULD**
7. Добить ~3000 RU за счёт MUST, не водой. EN ≤4000.
8. Роутер: как проверить, что он жив — `systemctl is-enabled buzz-message-router` + `systemctl status`. Иначе урок «6 дней inactive» нельзя применить.
9. Identity injection — одно действие: инжектить личность в вызов роутера (отдельный keypair + system prompt на старте процесса), не абзацем в канале.
10. Watcher: cron `*/15`, алерт в тот же `#agent-bus`. Сейчас «каждые 15 минут» — намерение, не рецепт.

**NICE**
11. Relay: одна стартовая команда (`docker compose up -d`), без домена.
12. Каналы: писать **11**, не «~11».
13. Хештеги убрать или оставить только `#Nostr #Buzz`. `#BuildingInPublic` — маркетинг.

### Честно

Дочитал бы. Украл бы пункты 2, 4, 6, 7 — dual addressing и mention-gate стоят поста. С нуля стек не поднять: нет `stop-all`, нет куда ставить `--reasoning none`, нет проверки systemd. Для X-поста это терпимо после MUST. Не сказка, не «я победил». После фикса — можно в EN.
